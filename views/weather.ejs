<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Weather</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <link rel="stylesheet" href="style.css">
</head>
<body style="<% if (mainWeather === 'Clouds') { %>background-image: url('cloudy-background.jpg');<% } else if (mainWeather === 'Rain') { %>background-image: url('rainy-background.jpg');<% } else if (mainWeather === 'Clear') { %>background-image: url('clear-background.jpg');<% } else if (mainWeather === 'Snow') { %>background-image: url('snow-background.jpg');<% } else { %>background-image: url('default-background.jpeg');<% } %>">

<div class="container">
  <div class="weather-side">
    <div class="weather-gradient"></div>
    <div class="date-container">
      <h2 class="date-dayname"><%=capitalizedDay%></h2><span class="date-day"><%=formattedDate%></span><span class="location"><%=city%>  📍</span>
    </div>
    <div class="weather-container"><i class="weather-icon" data-feather="sun"><img src="<%= imgUrl %>" alt="Weather icon"></i>
      
      <h1 class="weather-temp"><%= temp %>°C</h1>
      <h3 class="weather-desc"><%= capitalizedWeatherDes %></h3>
    </div>
  </div>
  <div class="info-side">
    <div class="today-info-container">
      <div class="today-info">
        <div class="precipitation"> <span class="title">FEELS LIKE</span><span class="value"><%= feels_like %> °C</span>
          <div class="clear"></div>
        </div>
        <div class="humidity"> <span class="title">HUMIDITY</span><span class="value"><%= humidity %> %</span>
          <div class="clear"></div>
        </div>
        <div class="wind"> <span class="title">WIND</span><span class="value"><%= wind %> mp/h</span>
          <div class="clear"></div>
        </div>
      </div>
    </div>
    <div class="week-container">

    
      <div class="city-input">
        <form action="/" method="post">
          <label for="city" >TRY ANOTHER CITY</label>
          <input type="text" id="city" name="cityName" placeholder="Enter city name...">
        
      </div>
      </div>
      <div class="location-container">
        <button type="submit" class="location-button"> <i data-feather="map-pin"></i><span>Change location</span></button>
      </div>
    </form>
  </div>
</div>

</body>
